# --- Dependency Directories ---
node_modules/
.pnpm-store/       # pnpm's global store (if local)
.pnpm/             # pnpm's virtual store (in project)
pnpm-lock.yaml     # pnpm's lockfile (if you prefer to keep it tracked)

# --- Next.js ---
.next/             # Next.js build cache and output
.next/cache/       # (explicitly, if needed)

# --- Build & Output ---
dist/              # Compiled TypeScript/JavaScript
build/             # Alternative build directory
out/               # Next.js static export (if using `next export`)
coverage/          # Test coverage reports
*.tsbuildinfo      # TypeScript incremental build info

# --- Environment & Secrets ---
.env               # Environment variables
.env.local         # Local overrides
.env.*             # Other env files (e.g., .env.prod)
*.env              # Additional env files
secrets.json       # Sensitive configs

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-error.log*

# --- IDE & Editor ---
.vscode/
.idea/
*.swp

# --- OS-Specific ---
.DS_Store
Thumbs.db
desktop.ini

# --- Testing ---
/junit.xml
/test-results/

# --- Misc ---
.cache/
.temp/
dump.rdb